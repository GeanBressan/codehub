<div>
    <a href="{{ route('profile.followers', $username) }}" class="text-sm text-gray-500 hover:text-emerald-600"><i class="fas fa-user-friends mr-1"></i> Seguidores
        <span class="font-semibold">{{ $followersCount }}</span></a>
</div>
